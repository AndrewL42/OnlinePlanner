@model IEnumerable<OnlinePlanner.Models.SignIn>
@*@model SignIn*@
@*@using static OnlinePlanner.Models.SignIn*@

@{
    ViewData["Title"] = "Sign In";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Username)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Password)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Email)
            </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>

@foreach (var item in Model)
{
    System.Diagnostics.Debug.WriteLine(Model);
    System.Diagnostics.Debug.WriteLine("INDEX: " + item);
}

        <tr>
            <form method="post" asp-controller="SignIn" asp-action="Login">
                <td>
                    <input type="text" name="username" />
                </td>
                <td>
                    <input type="password" name="password" />
                </td>
                    @*<label asp-for="a"></label>*@
                    @*<input asp-for="Username"/>*@
                    @*<span asp-validation-for="Username"></span>*@
                @*@Html.HiddenFor(modelItem => item.Username)
                @Html.HiddenFor(modelItem => item.Password)*@
                <td>
                    <input type="submit" value="Login" />
                </td>
            </form>
            @*<td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Password)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
        </td>*@
        </tr>   
@*  }
    </tbody>
</table>*@

